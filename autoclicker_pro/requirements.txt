# ไลบรารีหลักที่จำเป็นต่อการรันโปรแกรม
pyautogui>=0.9.54
pynput>=1.7.6
pillow>=10.0.0

# สำหรับหน้าตาทันสมัย (Sun Valley theme) — ถ้าไม่ติดตั้งโปรแกรมจะ fallback เป็นธีมพื้นฐานอัตโนมัติ
sv-ttk>=2.6.0

# สำหรับไอคอน System Tray — ถ้าไม่ติดตั้งฟีเจอร์ tray จะถูกปิดใช้งานอัตโนมัติ
pystray>=0.19.5

# จำเป็นเฉพาะตอน "แปลงเป็น .exe" เท่านั้น ไม่ต้องติดตั้งถ้าแค่รันด้วย python
pyinstaller>=6.0.0

pynput>=1.7.6
